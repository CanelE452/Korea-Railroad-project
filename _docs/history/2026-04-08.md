# 2026-04-08

## v8 Ablation 학습 완료 (A/B/C/D/E)

모든 ablation은 mixed_v8 (ep60) 위에 mixed_v8_train (9000장)으로 5 epoch finetune.

```
Ablation   Loss 설정                        Weight 경로
─────────────────────────────────────────────────────────────────────────
A (coord)  struct_coord=0.005               weights/v9_ablation_A_coord/
B (edge)   struct_edge=0.05                 weights/v8_ablation_B_edge/
C (co+ed)  struct_coord=0.005, edge=0.05    weights/v8_ablation_C_coord_edge/
D (flip)   struct_flip=0.02                 weights/v8_ablation_D_flip/
E (rel)    rel_loss, lambda=0.005, log=0.5  weights/v8_ablation_E_rel/
```

### A vs E 차이
- A: `Huber(||mu_i - gt_i|| / D)` — 모든 keypoint 동일 가중치
- E: `Huber(||mu_i - gt_i|| / D) / sigma_i + 0.5*log(sigma_i)` — heatmap에서 sigma(uncertainty) 추출, 점별 가중치 다르게

## infer_and_filter_v2.py 수정

### Filter A 저장 추가
- Before: A 점수는 CSV에만 기록, 폴더 미저장
- After: `A/` 디렉토리 추가, fA pass/fail 판정 + overlay 저장
- CSV에 `fA` 컬럼 추가, summary 출력에 A 통계 추가
- 파일: `scripts/data_prep/infer_and_filter_v2.py`

## noapril 5모델 ablation 비교

```
             PnP Rate    A pass    B pass    C pass    B∧C
v8 (base)    49.5%       43장      1장       0장       0장
A (coord)    62.2%       19장      16장      6장       6장  ★ B∧C 최고
B (edge)     54.3%       43장      20장      4장       4장
C (co+ed)    55.3%       31장      13장      2장       0장
D (flip)     29.3%       19장      7장       2장       1장
E (rel)      62.8%       24장      25장      4장       2장  ★ PnP/B 최고
```

### 핵심 발견
- **coord loss (A)가 B∧C 기준 최고** — 좌표 직접 비교가 양쪽 endpoint + PnP 안정성에 가장 효과적
- **edge loss (B)는 B pass 최다(20)** 이지만 C pass에서 밀림 — 변 길이는 맞추지만 위치 정밀도 부족
- **coord+edge (C)는 합쳤는데 오히려 악화** — edge loss가 coord loss와 간섭
- **flip (D)는 최악** — PnP 49.5→29.3%로 성능 해침
- **rel (E)는 PnP/B에서 최고** — uncertainty weighting이 분포를 넓히지만, C(LOO 안정성)에서 약함
- A를 core gate에서 뺀 설계 재확인 — coord/rel loss가 flip equivariance를 깨뜨림

## v8ablationa real_data 추론 결과

```
Total:    1924장
PnP OK:   1522 (79.1%)
A pass:    152 (10.0%)
B pass:    340 (22.3%)
C pass:    554 (36.4%)
B∧C:       222 (11.5%)
```

## mixed_v10 noapril 추론

- Weight: `weights/mixed_v10/final_net_epoch_0060.pth`
- PnP 3.7% (7/188) — noapril에서 매우 나쁨

## v10_a noapril 추론

- Base: mixed_v10 (ep60), Data: mixed_v9_train, coord loss 5 epoch
- Weight: `weights/v10_a_coord/final_net_epoch_0065.pth`

```
             PnP Rate    B pass    C pass    B∧C
v10          3.7%        0장       0장       0장
v10_A        44.1%       3장       0장       0장
```

- v10(3.7%) → v10_A(44.1%)로 개선되었지만, v8_A(62.2%)에 못 미침
- v10 base 자체가 noapril에서 약하기 때문

## 비교 스크립트 생성

- `scripts/data_prep/compare_filters.py` — 2모델 필터별 나란히 비교
- `scripts/data_prep/compare_filters_5models.py` — 5모델(v8/A/B/C/D) 나란히 비교
- `scripts/data_prep/compare_filters_3models.py` — 3모델(v8/A/E) 나란히 비교
- 출력 경로:
  - `data/pallet/eval_results/v8_vs_v8ablationa_compare/{A,B,C}/`
  - `data/pallet/eval_results/ablation_5model_compare/{A,B,C}/`
  - `data/pallet/eval_results/v8_A_E_compare/{A,B,C,BC}/`

## training_loss.md 업데이트

- Structural Loss 섹션 추가 (soft-argmax vs argmax 설명, coord/edge/flip 3요소, ablation 결과)
- 파일: `_docs/models/training_loss.md`
