# 2026-05-07 — GitHub 공개 준비

## requirements.txt 누락 패키지 추가

코드에서 import 되지만 `requirements.txt`에 없던 패키지 6종 추가.

```
ML Utilities 섹션:
  + tensorboard           # scripts/launch_tensorboard.py
  + tensorboardX          # DOPE 학습 로깅 (Deep_Object_Pose)
  + matplotlib            # 시각화 전반

Pose / Transforms 섹션:
  + pyquaternion          # pose math
  + pupil-apriltags       # AprilTag GT 생성 (scripts/data_prep/apriltag/)
  + randomcolor           # 시각화
```

버전 핀 없음 — 향후 호환성 이슈 발생 시 핀.

## GitHub 공개 준비 — 완료

- `origin` 변경: `NVlabs/FoundationPose.git` → `CanelE452/pallet-6d-pose.git`
- weights/ → Hugging Face Hub `CanelE452/pallet-pose-dope-weights` (3 핵심 ckpt)
- data/ 는 .gitignore 유지
- History squash: NVlabs 40 commits + migration 1 → 단일 initial commit
- `Deep_Object_Pose/.git` 삭제 → 일반 디렉토리로 편입
- `.mcp.json` untrack (machine-specific path)

## Docker 인프라 제거

실제 워크플로 검토 결과 Docker 는 한 번도 사용된 적 없음 (모든 작업이 Windows
native + conda `pallet-pose` 에서 수행). 미사용 인프라 정리:

- 삭제: `docker/`, `docker-compose.yml`, `.devcontainer/`, `scripts/launch_v2.ps1`
- 유지: `scripts/dope/run_dope_live.py` — native 실행 가능 (`pip install
  pyrealsense2` + Intel RealSense SDK 만 있으면 됨, lazy import 구조)
- README + CLAUDE.md (root, scripts/, scripts/dope/) 의 Docker 언급 → native
  실행 방식으로 변경
