# 2026-05-20

## baseline_v8_A 시퀀스 진단 — capturepallet09 전 프레임 z-gate fail

### 실행 명령
```bash
PYTHONUTF8=1 python challenge/scripts/run_live.py \
    --seq data/outside/capturepallet09 \
    --seq_fps 5 --seq_loop
```
(`--weights` 미지정 → `challenge/config/task.yaml` 의 `baseline.weights = challenge/weights/baseline_v8_A.pth` 사용)

### 결과 (60 frame 처리 후 사용자가 종료)
```
[Seq]   capturepallet09 — 2773 frames, fx=614.2 fy=614.3
[모델]  challenge/weights/baseline_v8_A.pth (로딩 1.66s)
[Gates] min_kp=7  max_reproj=8.0px  z=[0.3, 5.0]m  depth_rel<0.30

[F0]   thr=0.300 | results=1 best=N confirm=0/2 | last_reason=z=7.59m out of range
[F60]  thr=0.300 | results=1 best=N confirm=0/2 | last_reason=z=5.38m out of range
```

### 진단
- DOPE detection 자체는 매 프레임 1개 나옴 (`results=1`)
- 그러나 PnP 결과 z 가 5~7m → max_z=5.0m gate 에서 reject
- **CONFIRMED 0 건** — 사실상 사용 불가
- 원인: baseline_v8_A 학습 데이터 (`mixed_v8_train`, dims=1.10×1.10×0.15) ≠ challenge 환경 (plastic pallet, 1.10×1.30×0.11). PnP 가 사용자 plastic 팔레트를 KS T-11 dims 로 풀어서 z 추정이 비현실적으로 멀어짐.
- 5/14 `sweep_capturepallet01.json` 의 z=9~256m 패턴과 동일 증상

### 사용 도구 / cp949 인코딩 우회
- 1차 시도 (UTF-8 미지정) → `UnicodeEncodeError: 'cp949' codec can't encode character '—'`
- `PYTHONIOENCODING=utf-8 PYTHONUTF8=1` 추가 후 정상 동작

## scripts/train_dope.sh — 멀티 디렉토리 + env override 지원

baseline 진단 결과로 새 학습이 필요해서, 여러 데이터 폴더를 한 번에 학습할 수 있게 train_dope.sh 보강.

### 변경
- `--train_dirs "dir1 dir2 dir3"` 옵션 추가 (space-separated, 우선순위 ≥ `--train_dir`)
- `BATCH`, `WORKERS` 환경변수 override 추가 (기존 `EPOCHS`, `LR`, `MIN_IMAGES` 와 동일 패턴)
- `TRAIN_COUNT` 를 모든 dir 에서 재귀 탐색하여 합산 (`find $d -name '*.png'`)
- `--data` 부분을 `for d in $TRAIN_DIRS; do echo "../../$d"; done` 로 펼침

근거: `Deep_Object_Pose/train/train.py:505` 의 `--data nargs="+"` + `Deep_Object_Pose/common/utils.py:78-115` `loadimages()` 재귀 탐색 + `utils.py:229-230` `CleanVisiiDopeLoader` 가 path 리스트 지원. **symlink 불필요**.

### 변경 파일
- `scripts/train_dope.sh` — 4군데 패치 (인자 파싱 / WORKERS env / TRAIN_DIRS 처리 / TRAIN_COUNT 합산 / DATA_ARGS 펼침)

## DOPE 학습 시작 — combined_v8_v1_v2 (scratch, 60 ep)

### 데이터 합산 (실측, loader 기준)
```
data/pallet/training_data/mixed_v8_train                9,000  pairs   (PNG+JSON 짝)
challenge/data/training/v1 (part_000~008 + part_009 일부)  9,992  pairs
challenge/data/training/v2 (part_000~008 + part_009 일부)  9,988  pairs
─────────────────────────────────────────────────────────────────
합계                                                    28,980 pairs
```

**part_010~015 는 overlay 이미지 (PNG) 만 있고 JSON 없음** → 학습 불가, loader 가 자동 제외. `find -name '*.png'` 로 단순 카운트하면 PNG 49K 로 보이나, 실제 학습 데이터는 28,980 pairs. 원래 plan 추정 29K 와 일치.

### KeyError 'visibility' 패치 (`Deep_Object_Pose/common/utils.py:283`)
- mixed_v8 JSON 에는 `visibility` 필드 있음 (5/14 docs 에서 추가)
- challenge v1/v2 JSON 에는 `visibility` 없음 (대신 `num_corners_visible`, `keypoint_in_frame` 등)
- 패치: `obj["visibility"] > 0` → `obj.get("visibility", 1) > 0`
- 효과: visibility 필드 없는 데이터셋도 학습 가능 (default = visible)

### Convention 확인 (세 데이터셋 일치)
세 데이터셋 모두 `0 = front-top-RIGHT` (OpenCV convention). 학습 호환 OK.
```
mixed_v8 [0]:(325.9, 240.3) [1]:(194.1, 238.4)   ← 0 이 1 보다 오른쪽
v1       [0]:(89.9, 195.6)  [1]:(-42.9, 210.5)   ← 동일
v2       [0]:(173.8, 220.9) [1]:(129.7, 237.0)   ← 동일
```

### 학습 설정
```
exp_name      challenge
init weight   scratch (VGG-19 pretrained=False, baseline_v8_A 와 동일 조건)
data          mixed_v8_train + challenge/training/v1 + v2 (48,995 pairs)
batch_size    8        (baseline 4 → 2x, 사용자 결정)
lr            1e-4     (mixed_v8 baseline 동일, sqrt scaling 안 함)
epochs        60       (사용자 결정, 30ep ckpt 부터 사용 가능)
imagesize     448      (baseline 동일)
sigma         4.0      (CLAUDE.md gotcha: sigma<1 vanishing)
workers       4        (baseline 0 → I/O 병목 해소)
save_every    5        (12 ckpt: ep5,10,15,...,60)
PYTHONUTF8    1        (cp949 인코딩 에러 방지)
```

### 실행 명령
```bash
conda activate pallet-pose

EXP_NAME=challenge \
BATCH=8 \
WORKERS=4 \
LR=1e-4 \
EPOCHS=60 \
PYTHONIOENCODING=utf-8 \
PYTHONUTF8=1 \
bash scripts/train_dope.sh \
    --train_dirs "data/pallet/training_data/mixed_v8_train challenge/data/training/v1 challenge/data/training/v2"
```

### 예상 소요시간 (RTX 3080, 10GB)
```
요소                              영향
─────────────────────────────────────────────────
baseline: 9K pairs / b=4 / w=0    10 min/ep (실측)
데이터 29K (3.22x)                 ×3.22 → 32 min/ep
batch 8 (×0.6 실효)                ×0.6 → 19 min/ep
workers 4 (×0.7 I/O 해소)          ×0.7 → 13 min/ep
─────────────────────────────────────────────────
60 epoch × 13 min ≈ 13 시간       (ep30 ckpt 는 ~6.5시간 후)
```

GPU 확인: total 10240 MiB / free 8974 MiB → batch=8 + img=448 충분.

### 학습 재시작 사고 (06:40~06:55)

1차 `EXP_NAME=challenge` env 시도 → train_dope.sh line 40 `EXP_NAME=""` 가 env 를 덮어써서 `output_dir=weights/pallet_category` 로 잘못 저장 시작. 06:40:57 에 `weights/pallet_category/header.txt` 가 새 메타로 **덮어써짐** (ckpt 12 개는 03-20 mtime 그대로 손상 X). 학습 즉시 kill.

**복구 작업**:
- `scripts/train_dope.sh:40` — `EXP_NAME="${EXP_NAME:-}"` 로 패치 (env 우선)
- `weights/pallet_category/header.txt` — `_docs/models/archive.md` 메타 + 사고 노트로 복구

2차 재시작 (`--exp_name challenge` CLI) → 정상 시작, ep0 19% (5,600 image, 4분 19초) 까지 진행. iter time stabilize 결과 **22 min/ep** (plan 추정 13 min 의 1.7x). 그 직후 `OSError: unrecognized data stream contents` 로 다시 죽음.

**원인**: 깨진 PNG 12 개 (v1 6, v2 6). PIL 이 못 읽음. 격리:
```
data/pallet/training_data/_broken_png_quarantine_2026-05-20/   # 24 파일 (PNG+JSON 쌍)
challenge/data/training/v1/part_002,003,005,008/...   # 6 개 제거
challenge/data/training/v2/part_002,004,005,007,008/...   # 6 개 제거
```
학습 데이터 28,980 → 28,968 pairs (영향 0.04%).

3차 재시작 (b4rqeawyr) — 같은 명령, 격리 후. 진행 중.

### 실측 시간 재추정 (iter time stabilized)
```
800 image / 88s  (첫 100 iter, cudnn warmup) → 9.1 img/s
5600 image / 259s (iter 700, stabilized)    → 21.6 img/s

→ 1 epoch ≈ 22.4 min (plan 13 min 의 1.7x, batch 8 scaling 실효 0.4x)
→ 60 epoch ≈ 22 시간
→ ep30 ckpt ≈ 11 시간 후 (현재 07:00 기준 → 18:00)
```

### TODO (학습 후)
- [ ] ep30 ckpt 나오면 `challenge/scripts/run_live.py --weights weights/combined_v8_v1_v2/net_epoch_0030.pth --seq capturepallet09` 로 z gate 통과 확인
- [ ] 최종 ep60 ckpt + 자동 PCK eval 결과 기록
- [ ] best epoch 선택 (sweep) — baseline_v8_A 의 ep65>ep68 케이스 (4/9) 재현될 수 있음
- [ ] `challenge/config/task.yaml` 의 `baseline.weights` 갱신 여부 결정

### 변경 / 추가 파일
- `_docs/history/2026-05-20.md` (이 문서)
- `scripts/train_dope.sh` (멀티 dir + env override 패치)
- `weights/combined_v8_v1_v2/` (학습 산출물, 진행 중)
