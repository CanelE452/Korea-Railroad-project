# 2026-05-25

## annotate.py truncation fix v7 + manual GT 대량 라벨링 + challenge0123 fine-tune

### annotate.py fix v7 (sentinel 버그)
근본 버그 — `_reproj_err_dict` 의 `if u < 0: errs.append(1e6)` 가 잘못된 sentinel 검사. `project_3d` 의 실제 sentinel = `(u, v) == (-1, -1)` (cam.z≤0). image 좌측 밖 점을 정확히 외삽한 경우 valid projection 의 u 도 음수 → 모든 candidate 1e6 균일 → tie-break 망가짐 → degenerate cuboid (z=4m+작은cube) 채택.

수정 (`challenge/scripts/annotate_pnp.py`):
- sentinel `(u, v) == (-1, -1)` 등호 검사로 정정
- `extrapolated_mask` 추가: t/x 외삽 점은 PnP weight 0.3 (직접 click=1.0)
- degenerate cuboid reject (img_shape 기반 bbox area threshold)

UI 변경 (`annotate_draw.py`):
- 외삽 점 = 속 빈 원 + `{idx}*` 표기 (직접 click=채워진 원)

새 키 추가 (`annotate.py`):
- **`x`**: active idx parallelogram 외삽 (같은 face 의 다른 3 corner → 4th = a+(c-b))
- **`g`** 완화: 0~5 6점 강제 → 4+ 점 + PnP 풀이 가능 시 동작

검증 (capturepallet03/1778651573855324672, truncation case):
- reproj_click 18.11 → 3.38px (5.4x 개선), z 3.99→3.56m
- regression test: cp03 batch 3/3 PASS mean 3.12px

### manual GT 라벨링 진행
v7 fix 적용된 도구로 14 시퀀스 라벨링:
```
시퀀스                    frame수
─────────────────────────────────
capturepallet02             5
capturepallet03             8
capturepallet04             6
capturepallet05             5
capturepallet07            27  (v6 도구로 재라벨, v4 결정 이전 LR-reversed 데이터 정정)
capturepallet08            20
capturepallet09            36
capturepalletcad           22
─────────────────────────────────
outside 누적              129
```

### challenge0123_ft_manual 학습
첫 fine-tune (104 frame, 20 ep):
```
EXP_NAME    : challenge0123_ft_manual
Data        : 6 폴더 (cp03/04/05/07/09/cad), 104 frame
Init        : weights/challenge0123/final_net_epoch_0060.pth (60 ep scratch)
Epochs      : 80 (start=61, end=80, 추가 20 ep)
LR          : 1e-4
Batch       : 8, Workers : 4
시작        : 05/24 23:46
종료        : 05/24 23:54  (7분 50초)
ckpt        : final_net_epoch_0080 + sweep 65/70/75/80 (4 sweep)
Loss        : ep 61: 0.0377 → ep 80: 0.0257 (마지막 5ep mean=0.0243, std=0.0015)
```

### 변경/추가 파일
- `challenge/scripts/annotate_pnp.py` (v7 sentinel + extrapolated_mask + degenerate reject)
- `challenge/scripts/annotate.py` (x 키 + g 완화)
- `challenge/scripts/annotate_draw.py` (외삽 표시)
- `challenge/scripts/annotate_io.py` (extrap_mask 필드)
- `challenge/scripts/plot_ft_loss.py` (loss 곡선 PNG)
- `weights/challenge0123_ft_manual/` (5 ckpt)
- 14 폴더: `challenge/data/capturepallet*_manual_gt/`
