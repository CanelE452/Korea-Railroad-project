# 2026-07-23

## 코드 변경 요약 (파일별 Before/After)

### `25y_automatic_lifter-master/25y_automatic_lifter-master/depth_cam/calib/config.py`
DOPE 6D 가중치 기본 경로를 삭제된 `challenge/model/` → challenge-free `weights/dope_6d/` 로 변경.
- Before:
  ```python
  model_dir = _REPO_ROOT / "challenge" / "model"
  pths = sorted(model_dir.glob("*.pth")) if model_dir.is_dir() else []
  if not pths:
      return str(model_dir / "challengenight.pth")
  if len(pths) == 1:
      return str(pths[0])
  for p in pths:
      if p.name == "challengenight.pth":
          return str(p)
  return str(pths[0])
  ```
- After:
  ```python
  model_dir = _REPO_ROOT / "weights" / "dope_6d"
  pths = sorted(model_dir.glob("*.pth")) if model_dir.is_dir() else []
  if not pths:
      return str(model_dir / "dope_6d.pth")
  return str(pths[0])
  ```
- 이유: challenge 폴더 삭제로 기본 가중치 경로가 사라짐. 리프터 통합 루프(main_rec.py)가
  challenge 없이도 동작하도록 repoint. `MODEL_PATH_6D` 환경변수 우선 로직은 유지.
  docstring/주석의 challenge 언급 및 challengenight.pth 특화 로직도 제거. (py_compile OK)

### 리프터 가중치 폴더를 25y_automatic_lifter-master 안으로 이동
사용자 요청: `.pth` 넣을 폴더를 `25y_automatic_lifter-master/` 안에 두기.
- 신규 폴더: `25y_automatic_lifter-master/models/` (+ 사용법 README.md). outer 25y 레벨
  = `_CONFIG_DIR.parents[2]`. `models/` 는 .gitignore 미해당(폴더·README 추적 가능, .pth 만 제외).
- `depth_cam/calib/config.py:_resolve_model_path_6d` 기본 dir 재변경:
  - Before: `model_dir = _REPO_ROOT / "weights" / "dope_6d"`  (프로젝트 루트, 직전 커밋 값)
  - After:  `model_dir = _CONFIG_DIR.parents[2] / "models"`   (= 25y_automatic_lifter-master/models)
  - `MODEL_PATH_6D` 환경변수 우선은 유지. 주석/docstring 도 새 경로로 갱신.
  - 검증: 폴더 존재 True, 경로해석 = .../25y_automatic_lifter-master/models/, py_compile OK.

### `scripts/data_prep/eval/evaluate_on_val.py`
- Before: `sys.path.insert(0, os.path.join(..., "challenge", "scripts"))` (line 43)
- After: 해당 줄 삭제.
- 이유: challenge 삭제로 죽은 경로. 실제 import(pnp_solver)는 self_training 에서 해결되어 무해하나 잔재 제거.

### `scripts/CLAUDE.md`, `scripts/data_prep/CLAUDE.md`
- 트리·표에서 삭제된 스크립트(inference v1~v3, filter_compare, 미등록 일회성) 반영. 정리 요약 주석 추가.


## Jetson 배포 관련 폴더 삭제

사용자 요청("젯슨나노 관련한거 없애줘" → "둘다")으로 Jetson 배포/문서 폴더 전체 제거.

### 삭제 (git rm -r, 커밋은 미실행 — 스테이징만)
- `challenge/_docs/jetson_nano/` (5파일: YOLO-pose 모델, TensorRT export FPS, FSM 통합, 배포 체크리스트, README)
- `jetson_fsm_deploy/` (4파일: CAN_PORTING.md, README, build_engine.sh, preflight.py)
- `pallet_jetson_deploy/` (4파일: README, build_engine.sh, infer_fps.py, requirements.txt)

### 이유
프로젝트 정리 중 Jetson(Nano 포함) 배포 자산을 걷어내기로 함. 핵심 CAN 제어 코드
(`25y_automatic_lifter-master/25y_automatic_lifter-master/CAN/`)는 별도 위치라 영향 없음.

### 미삭제 (판단 보류)
- `challenge/scripts/measure_latency.py` — 범용 DOPE latency 측정 스크립트. 내부에 Jetson
  예상 FPS 표(56~62줄)만 포함. Jetson 전용이 아니라 유지, 사용자 재확인 대기.

## scripts/ 미사용 파일 정리 (40개 삭제)

`data_prep/CLAUDE.md` 대장과 참조 그래프(basename 참조 0건 + __main__ 여부)로 미사용 판정.
불일치 발견: 문서상 `eval/`=4파일인데 실제 30파일(2026-06-04 일회성 분석 다수 미등록),
문서가 명시한 `debug/` 폴더는 실재하지 않음.

### 삭제 (git rm, 커밋 미실행 — 스테이징만), 사용자 승인 [A]+[B]+[C]
- **[A] 미등록 일회성 25개**: eval/ 의 plot_*(13), dump_*(2), filter_combo_*_overlay/perkp,
  filter_domain_overlay, extra_filter_analysis, pl_gt_diff_analysis, diag_pass_contact_sheet,
  prototype_demo, px_to_cm, qualitative_panel, validate_nn_metric + 루트 plot_epoch_sweep.py
- **[B] legacy 버전중복 6개**: inference/ infer_and_filter{,_v2,_v3}(v4가 canonical 최종 유지),
  filter_compare/ 3개(filter_pr_eval 로 통합됨 → 폴더 소멸)
- **[C] 업로드/기타 유틸 9개**: upload_weights_hf, hf_upload_cropaug_truncation,
  self_training/smoke_test, inference/extract_pl_v5, visualize/{dope_single_image,
  dope_slide_figure,regen_overlay_no_cuboid,progression_chart}, blender/render_blender_data

### 안전 확인
삭제 전 [B]/[A] 대상이 다른 .py 에서 import 되는지 grep — 전부 import 참조 없음 확인 후 삭제.

### 문서 동기화
- `scripts/CLAUDE.md`, `scripts/data_prep/CLAUDE.md` 의 트리·inference/filter_compare 표 갱신.

### 유지
canonical_filters, visualize_inference(공유 lib), evaluate_*, filter_pr_eval, quick_screen,
self_train*, sdg_*, apriltag_gt* 등 정식/참조 있는 파일.

## challenge/ 폴더 삭제 (116파일)

사용자 요청으로 과제 전용 워크스페이스 `challenge/` 통삭제 (git rm -r, 커밋 미실행).

### 사전 확인 (삭제 전 의존성 조사)
- CAN 통신(`CAN/control_forklift*.py`): challenge 비의존 → 삭제 후 정상.
- 파렛트 6D 추론: 핵심 모듈(DopeNetwork, cuboid_pnp_solver=Deep_Object_Pose/common,
  pnp_solver=self_training, visualize_inference)이 전부 challenge 밖 → `.pth`만 있으면
  `scripts/dope/run_dope_live.py`(challenge 참조 0)로 추론 가능.
- 잃는 것: 추론+gate판정+CAN을 묶은 통합 실배포 glue(`run_live*`, `config/task.yaml`)와
  depth_cam fusion 경로. 사용자가 트레이드오프 인지 후 삭제 결정.

### 삭제로 깨지는 dangling 참조 (남은 코드가 challenge 참조)
- `depth_cam/calib/config.py:58` — DOPE 가중치 dir = challenge/model/ (예정된 손실)
- `depth_cam/verify_depth_fusion.py:53` — challenge/data GT 경로
- `scripts/ft_dope_cropaug.sh:17~47` — challenge/data 실촬영 파인튜닝 데이터(데이터째 삭제)
- `scripts/data_prep/eval/evaluate_on_val.py:43` — 죽은 sys.path.insert(무해, import은
  self_training 에서 해결) → 정리 대상
- 그 외 주석/help 문자열 다수(동작 무관): config.py, main_rec.py, dope_inference.py,
  Deep_Object_Pose/common/utils_dataset.py, train/train.py

### 후속 정리 (완료)
- `evaluate_on_val.py:43` challenge/scripts 죽은 sys.path.insert 제거.
- `scripts/ft_dope_cropaug.sh` 삭제 (challenge 실촬영 데이터 의존, 데이터도 삭제됨).
- 미처리(사용자 선택): depth_cam 은 보존, 프로젝트 CLAUDE.md challenge 서술 미갱신.

### challenge 없이 리프터 추론+이동 동작화 (사용자 목표)
통합 루프 = `25y_automatic_lifter-master/.../depth_cam/main_rec.py`
(RealSense + DOPE/YOLO 6D 추론 → CalibrationFSM align → CAN 제어). 조사 결과:
- 런타임 challenge 의존 = `depth_cam/calib/config.py` 의 DOPE 가중치 기본 경로 **한 곳뿐**.
  임계값(belief/gate)은 전부 config.py 하드코딩이라 challenge 안 읽음. backend 기본=dope.
- 정정: `_REPO_ROOT = _CONFIG_DIR.parents[3]` 은 **프로젝트 루트**(앞서 파일기준으로 잘못
  세어 "lifter 폴더/죽은 경로"라 오판했음. _CONFIG_DIR 은 디렉터리라 한 칸 차이).
  즉 `challenge/model` 은 실제 프로젝트루트 challenge 를 가리켰고, 삭제로 기본경로가 사라짐.
- 수정: `config.py:_resolve_model_path_6d` 기본 dir 을 `challenge/model` →
  `_REPO_ROOT/"weights"/"dope_6d"` 로 변경, challenge 특화 파일명(challengenight.pth) 로직 제거.
  `MODEL_PATH_6D` 환경변수 우선은 유지. (py_compile OK, 경로해석 검증 완료)
- 결과: challenge 없이도 `weights/dope_6d/` 에 .pth 두거나 `MODEL_PATH_6D` 지정 시
  리프터 추론+이동(main_rec) 동작. CAN·run_dope_live 는 원래부터 challenge 무관.

### 가중치(.pth) 배치 (사용자 질문 답)
- `weights/`, `data/`, `*.pth` 는 모두 .gitignore → 런타임 배치.
- 표준 위치 `weights/<name>/xxx.pth`. 대부분 진입점이 `--weights <path>` 인자라 임의 경로 가능.
  - 실시간 추론: `run_dope_live.py --weights <path>` (기본값 data/pallet/ndds3_pallet.pth)
  - 평가: `evaluate_on_val.py / evaluate_real.py --weights <path>` (필수)
  - depth_cam fusion: 환경변수 `MODEL_PATH_6D=<abs path>` 우선 → challenge/model 없이 작동
    (config.py `_resolve_model_path_6d` 가 env var 먼저 확인).

## 트럭 적재(post-insertion) 모델 zip → truck_loading/ 배치
사용자 설명: 현 프로젝트 = "파렛트 추론→삽입"까지, zip = 삽입 후 "리프터가 트럭을 찾아
트럭에 파렛트를 놓는" 단계. → 전용 폴더 `truck_loading/` 신설해 zip 이중압축 해제.
- `truck_loading/다이어그램.txt` — 전체 FSM(파렛트 픽업+트럭 적재) mermaid 다이어그램
- `truck_loading/geometry_v2_live_camera_laser_ready_20260723/` — SMOKE geometry_v2
  (DLA-18-NODCN, DETECT_CLASSES=Car, dims 5.16×1.97×1.74 = 트럭/차량) + weights/model_final.pth(248MB)
  + 시리얼 레이저(TFmini-S) 실시간 추론 코드. 총 94파일 238MB.
- 루트에 있던 `트럭탐지모델+다이어그램.zip` 은 해제 후 제거(루트 방치 금지, 원본은 ~/Downloads 유지).
- 참고: 기존 `student_dla18_nodcn_640x480_fp32_with_inference_v4_20260723/` 와 같은 SMOKE
  geometry_v2 계열 → 중복 가능성 있음(사용자 확인 대기).

## 트럭탐지 모델 zip 반입
- `~/Downloads/트럭탐지모델+다이어그램.zip`(229MB)을 프로젝트 루트로 복사. `.gitignore` 대상.
- 내용: SMOKE geometry_v2(DLA-18-NODCN, DETECT_CLASSES=Car) + 시리얼 레이저(TFmini-S) 실시간 추론.
  기존 `student_dla18_nodcn_640x480_fp32_with_inference_v4_20260723/` 번들과 동일 모델 계열.
  (CAN 아님 / 팔레트 DOPE 아님 — 통신·탐지대상이 프로젝트 핵심과 다름.)
